# Revisions that only reformatted code. `git blame` skips past them so it
# still points at whoever wrote a line, not at the reformat.
#
# Enable locally with:
#   git config blame.ignoreRevsFile .git-blame-ignore-revs
#
# GitHub's web blame reads this file on its own; no setup needed there.

# Format the tree with clang-format for the first time
717100144b4edf1ffd2150d136fe8f6c60416b10

# Format the code the merge brought in
12f3af911a8cbda52794f72b05c3295f4f24c29a
