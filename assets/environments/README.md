# Environment Assets

Place an optional HDR environment at:

```text
assets/environments/studio.hdr
```

Large HDR assets are intentionally not committed. If `studio.hdr` is missing or fails to load, the renderer uses the procedural environment fallback.
